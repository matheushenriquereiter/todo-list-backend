info:
  name: Create Tag
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/tags"
  body:
    type: json
    data: |-
      {
        "name": "dsad"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
