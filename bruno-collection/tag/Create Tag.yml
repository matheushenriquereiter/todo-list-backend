info:
  name: Create tag
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/tags"
  body:
    type: json
    data: |-
      {
        "name": "aa"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
