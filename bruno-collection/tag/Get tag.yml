info:
  name: Get tag
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/tags/:tagId"
  params:
    - name: tagId
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
