info:
  name: Login user
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "matheus@gmail.com",
        "password": "matheus"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
