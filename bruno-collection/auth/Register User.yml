info:
  name: Register User
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "aaa",
        "email": "aaa@gmail.com",
        "password": "aaa"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
