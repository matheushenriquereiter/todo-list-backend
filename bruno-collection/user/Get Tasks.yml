info:
  name: Get Tasks
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/users/:userId/tasks"
  params:
    - name: userId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
