info:
  name: Login User
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/users/login"
  body:
    type: json
    data: |-
      {
        "email": "matheus@gmail.com",
        "password": "matheus123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
