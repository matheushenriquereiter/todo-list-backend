info:
  name: Update Task
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{url}}/tasks/:taskId"
  params:
    - name: taskId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "Pior de todas as tarefas",
        "description": "wow"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
