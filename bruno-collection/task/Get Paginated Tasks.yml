info:
  name: Get Paginated Tasks
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/tasks?limit=5&offset=0"
  params:
    - name: limit
      value: "5"
      type: query
    - name: offset
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
