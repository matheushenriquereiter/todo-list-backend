info:
  name: Delete task
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{url}}/tasks/:taskId"
  params:
    - name: taskId
      value: "3"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
