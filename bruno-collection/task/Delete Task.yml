info:
  name: Delete Task
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{url}}/tasks/:taskId"
  params:
    - name: taskId
      value: "2"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
