info:
  name: Update task status
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{url}}/tasks/:taskId/status"
  params:
    - name: taskId
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "status": "TO_DO"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
