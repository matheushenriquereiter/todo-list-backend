info:
  name: Update Task Status
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{url}}/tasks/:taskId/status"
  params:
    - name: taskId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "status": "DONE"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
