info:
  name: Create task
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/tasks"
  body:
    type: json
    data: |-
      {
        "title": "Melhor de todas as tarefas",
        "description": "wow"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
